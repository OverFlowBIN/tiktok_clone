import 'package:flutter/material.dart';

class MainNavigationNavogation extends StatelessWidget {
  const MainNavigationNavogation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
